# Null object pattern

The idea behind this pattern is to create an object instead of manually checking for properties in an object, so by using this pattern we only need to return this `null object` instead of checking for unexisting properties

<img src='https://reactiveprogramming.io/books/patterns/img/patterns-articles/null-object-diagram.png' />
